# Next.js inlines NEXT_PUBLIC_* vars into the client bundle at build time —
# the browser-side fan app talks to the backend directly (both its REST API
# and its Socket.io realtime channel), not through a Next.js API route.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

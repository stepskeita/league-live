# Next.js inlines NEXT_PUBLIC_* vars into the client bundle at build time —
# the browser-side admin panel talks to the backend directly, not through a
# Next.js API route.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
